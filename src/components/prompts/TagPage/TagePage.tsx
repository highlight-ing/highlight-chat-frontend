'use client'

import TrendingPrompts from '@/components/TrendingPrompts/TrendingPrompts'

export default function TagPage() {
  return (
    <TrendingPrompts userId={undefined} prompts={[]} pinnedPrompts={[]} openModal={() => {}} selectPrompt={() => {}} />
  )
}
